# ===================================================================
# 1. CONFIGURACIÓN DE SERVIDOR (PARA PRODUCCIÓN)
# ===================================================================
# Railway asigna el puerto automáticamente mediante la variable PORT
server.port=${PORT:8080}

# ===================================================================
# 2. CONFIGURACIÓN DE BASE DE DATOS (MYSQL RAILWAY)
# ===================================================================
# Se mantienen los datos de conexión que proporcionaste
spring.datasource.url=jdbc:mysql://switchback.proxy.rlwy.net:59392/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
spring.datasource.username=root
spring.datasource.password=hTBJvebzWdPeSvzsIjJcAqiMXUAodiBV
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# 3. JPA / HIBERNATE
# ===================================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===================================================================
# 4. CONFIGURACIÓN DE CORREO (GMAIL SMTP - PUERTO 465 SSL)
# ===================================================================
# Usamos las variables que ya configuraste en el panel de Railway (${NOMBRE_VARIABLE})
# Si no las reconoce, usará los valores por defecto que ponemos después de los dos puntos.
spring.mail.host=smtp.gmail.com
spring.mail.port=465
spring.mail.username=soportegandersync@gmail.com
spring.mail.password=${SPRING_MAIL_PASSWORD:nepfymbnlljsrsxs}
spring.mail.protocol=smtp

# Propiedades de seguridad obligatorias para el puerto 465
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com
spring.mail.properties.mail.smtp.socketFactory.port=465
spring.mail.properties.mail.smtp.socketFactory.class=javax.net.ssl.SSLSocketFactory
spring.mail.properties.mail.smtp.socketFactory.fallback=false

# Tiempos de espera (Ajustados para evitar el Timeout inicial)
spring.mail.properties.mail.smtp.connectiontimeout=15000
spring.mail.properties.mail.smtp.timeout=15000
spring.mail.properties.mail.smtp.writetimeout=15000

# ===================================================================
# 5. LOGGING Y DEBUG
# ===================================================================
logging.level.org.springframework.web=INFO
# Activa esto para ver el handshake SMTP en los logs de Railway
spring.mail.properties.mail.debug=true
