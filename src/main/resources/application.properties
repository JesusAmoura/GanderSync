# ===================================================================
# 1. CONFIGURACIÓN DE SERVIDOR (CRÍTICO PARA RAILWAY)
# ===================================================================
# Esto permite que Railway asigne el puerto dinámicamente. 
# Si falla, usará el 8080 por defecto.
server.port=${PORT:8080}

# ===================================================================
# 2. CONFIGURACIÓN DE BASE DE DATOS (MYSQL RAILWAY)
# ===================================================================
spring.datasource.url=jdbc:mysql://switchback.proxy.rlwy.net:59392/railway?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&rewriteBatchedStatements=true
spring.datasource.username=root
spring.datasource.password=hTBJvebzWdPeSvzsIjJcAqiMXUAodiBV
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# ===================================================================
# 3. JPA / HIBERNATE
# ===================================================================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# ===================================================================
# 4. CONFIGURACIÓN DE CORREO (GMAIL SMTP)
# ===================================================================
# IMPORTANTE: La contraseña nepfymbnlljsrsxs NO debe tener espacios.
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=soportegandersync@gmail.com
spring.mail.password=nepfymbnlljsrsxs
spring.mail.protocol=smtp
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.timeout=10000
spring.mail.properties.mail.smtp.connectiontimeout=10000

# ===================================================================
# 5. LOGGING (PARA DEPURAR EN RAILWAY)
# ===================================================================
logging.level.org.springframework.web=INFO
logging.level.com.example.demo=DEBUG
logging.level.org.hibernate.SQL=ERROR
